import { DyWidgetsDirective } from './dy-widgets.directive';

describe('DyWidgetsDirective', () => {
  it('should create an instance', () => {
    const directive = new DyWidgetsDirective();
    expect(directive).toBeTruthy();
  });
});
